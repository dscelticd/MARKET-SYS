# ✅ Market Flow — 간단 사용 가이드

## 딱 2단계입니다

---

## 1단계 — 처음 한 번만: 헬스체크

모든 설정이 올바른지 확인합니다.

**`헬스체크.bat`** 더블클릭 → 오류 없으면 준비 완료

> ✅ 항목만 보이면 그냥 사용하면 됩니다.
> ❌ 항목이 보이면 `.env` 파일의 해당 값을 확인하세요.

---

## 2단계 — 매일 사용: 배치파일 더블클릭

```
📁 market_flow 폴더
 ├── 🖱️ 아침 리포트+이메일.bat   ← 아침에 더블클릭
 ├── 🖱️ 저녁 리포트+이메일.bat   ← 저녁에 더블클릭
 └── 🖱️ 대시보드 실행.bat        ← 브라우저에서 보고 싶을 때
```

끝입니다.

---

## 각 배치파일이 하는 일

### 아침 리포트+이메일.bat (오전 실행 권장)
1. 글로벌 시장 데이터 수집 (NVIDIA, SK하이닉스, KOSPI 등)
2. AI가 12개 종목 등급 계산 (추천/안전/보통/주의/위험)
3. Claude AI가 아침 브리핑 리포트 작성
4. `data/reports/` 폴더에 저장
5. 이메일로 자동 발송

### 저녁 리포트+이메일.bat (오후 6시 이후 실행 권장)
- 위와 동일, 당일 결산 리포트 작성

### 대시보드 실행.bat
- 브라우저에서 `http://localhost:8501` 자동 열림
- 등급 카드, 차트, 리포트를 시각적으로 확인 가능

---

## 이메일 없이 리포트만 받고 싶다면

```
🖱️ 아침 리포트 실행.bat   (이메일 없이 파일만 저장)
🖱️ 저녁 리포트 실행.bat   (이메일 없이 파일만 저장)
```

저장 위치: `market_flow/data/reports/20260528_morning.md`

---

## 자동으로 매일 실행하려면

```
🖱️ 자동실행 설정.bat   ← 한 번만 더블클릭
```

> 이후 컴퓨터가 켜져 있으면 매일 **오전 7시** / **오후 6시 30분** 자동 실행됩니다.

---

## 등급 기준 한눈에 보기

| 등급 | 점수 | 의미 |
|------|------|------|
| 🟢 추천 | 75~100 | 우선 검토할 만한 상태 |
| 🔵 안전 | 55~74 | 리스크 낮고 안정적 |
| 🟡 보통 | 35~54 | 긍정·부정 신호 혼재 |
| 🟠 주의 | 15~34 | 단기 리스크 있음 |
| 🔴 위험 | 0~14 | 중대 리스크 감지 |

> ⚠️ 모든 등급은 참고용입니다. 실제 투자 결정은 본인이 하세요.

---

## 문제가 생겼을 때

| 증상 | 해결 |
|------|------|
| 창이 바로 닫힌다 | `헬스체크.bat` 실행 후 ❌ 항목 확인 |
| 이메일이 안 온다 | `.env`의 `SMTP_PASSWORD` 확인 (앱 비밀번호 16자리) |
| 데이터가 이상하다 | `.env`에서 `USE_MOCK_DATA=false` 인지 확인 |
| API 오류 | `.env`의 `ANTHROPIC_API_KEY` 확인 |
