# Market Flow — 클라우드(GitHub Actions) 실행용 최소 패키지
# 대시보드(Streamlit/Plotly)와 로컬 스케줄러(schedule)는 제외

# Claude API
anthropic>=0.40.0

# 환경변수 로드
python-dotenv>=1.0.0

# 실시간 시장 데이터
yfinance>=1.4.0

# chart_generator.py가 직접 import — yfinance/mplfinance의 전이 의존성으로 우연히
# 설치되고 있었으나, 두 패키지 중 하나가 pandas 의존을 바꾸면 조용히 깨질 수 있어 명시
pandas>=2.2.0

# 수급(외국인/기관/개인 순매매) 데이터 — 네이버 금융 스크래핑
requests>=2.31.0
beautifulsoup4>=4.12.0

# 캔들차트 이미지 생성 (주목 종목 이메일 첨부용)
# mplfinance는 PyPI에 정식(stable) 릴리즈가 없고 전부 알파/베타 태그만 존재 —
# ">=0.12.10"처럼 정식 버전을 요구하면 해당 버전이 없어 설치 자체가 실패한다
# (실제로 GitHub Actions 예약 실행이 이 때문에 실패함, 2026-08-09 확인).
# 프리릴리스를 명시적으로 허용하려면 하한 버전 자체를 프리릴리스 태그로 지정해야 함.
mplfinance>=0.12.10b0

